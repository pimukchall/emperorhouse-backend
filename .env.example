# ========= DEV =========
# คัดลอกบล็อกนี้ไปลงไฟล์ .env.development
NODE_ENV=development
PORT=4000
FRONTEND_BASE_URL=http://localhost:3000
DATABASE_URL="mysql://root:@localhost:3306/DEV_EMP_DATABASE"

JWT_ACCESS_SECRET=replace-with-random-dev-secret
JWT_REFRESH_SECRET=replace-with-random-dev-refresh
ACCESS_TTL_SEC=900
REFRESH_TTL_SEC=604800

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
MAIL_FROM="EMP One <no-reply@emperorhouse.com>"

UPLOAD_BASE_DIR=./upload

# ========= PROD =========
# คัดลอกบล็อกนี้ไปลงไฟล์ .env.production (เฉพาะกรณีรัน prod “ในเครื่อง/VM”)
# *บน Cloud (Vercel/Railway/Server) แนะนำตั้งค่าเป็น Environment Variables ของระบบแทนไฟล์นี้*
# NODE_ENV=production
# PORT=4000
# FRONTEND_BASE_URL=https://your-frontend.example.com
# DATABASE_URL="mysql://user:pass@host:3306/EMP_DATABASE"

# JWT_ACCESS_SECRET=replace-with-strong-prod-secret
# JWT_REFRESH_SECRET=replace-with-strong-prod-refresh
# ACCESS_TTL_SEC=900
# REFRESH_TTL_SEC=604800

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM="EMP One <no-reply@emperorhouse.com>"

# UPLOAD_BASE_DIR=./upload
