# ============================================
# 🌱  General Application Settings
# ============================================

# Environment: development | production
NODE_ENV=development

# API Server Port
PORT=4000

# Frontend base URL (ใช้สำหรับ CORS และ redirect URL)
FRONTEND_BASE_URL=http://localhost:3000

# ============================================
# 🛢️  Database Connection
# ============================================

# Local Development Database (MySQL)
DATABASE_URL="mysql://root:password@localhost:3306/dev_emp_database"

# ตัวอย่าง Production (อย่า commit ค่าจริง)
# DATABASE_URL="mysql://user:pass@host:3306/emp_database"

# ============================================
# 🔐  Authentication & Security
# ============================================

# JWT secrets — ควรเป็น string แบบ random (openssl rand -base64 48)
JWT_ACCESS_SECRET=replace-with-random-access-secret
JWT_REFRESH_SECRET=replace-with-random-refresh-secret

# Token lifespan (seconds)
ACCESS_TTL_SEC=900         # 15 นาที
REFRESH_TTL_SEC=604800     # 7 วัน

# bcrypt cost factor
BCRYPT_SALT_ROUNDS=10

# ============================================
# 📧  SMTP (Email Service)
# ============================================

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true

# Gmail account / App Password (สำหรับส่งอีเมล)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Display name ของผู้ส่งในอีเมล
MAIL_FROM="EMP One <no-reply@emperorhouse.com>"

# ============================================
# 📁  File Uploads
# ============================================

# โฟลเดอร์สำหรับเก็บไฟล์อัปโหลด (local)
UPLOAD_BASE_DIR=./upload

# ============================================
# 👤  Seeding (Initial System Accounts)
# ============================================
# → ใช้สำหรับสร้างบัญชีผู้ดูแลระบบ / QMR / IT ตอน seed DB
# → email เป็น optional (ไม่บังคับกรอก)

# --- Admin ---
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=Admin@12345

# --- QMR ---
SEED_QMR_USERNAME=qmr
SEED_QMR_EMAIL=qmr@example.com
SEED_QMR_PASSWORD=Qmr@12345

# --- IT Staff ---
SEED_IT_USERNAME=staff.it
SEED_IT_EMAIL=staff.it@example.com
SEED_IT_PASSWORD=Staff@12345

# ============================================
# 🌐  Production Notes (for .env.production)
# ============================================
# - ใช้ NODE_ENV=production
# - FRONTEND_BASE_URL ต้องเป็น HTTPS domain จริง
# - JWT_SECRET ควรเป็นค่า random ยาว (อย่าใช้ค่า dev)
# - SMTP_USER/PASS ตั้งเป็น account production หรือ service ที่ปลอดภัย
# - แนะนำ: ตั้งค่าบนระบบ Deploy เช่น Vercel / Railway แทนการเก็บไฟล์
#
# ตัวอย่าง:
#
# NODE_ENV=production
# FRONTEND_BASE_URL=https://app.example.com
# DATABASE_URL="mysql://user:pass@prod-db-host:3306/emp_database"
# JWT_ACCESS_SECRET=base64-48-random-string
# JWT_REFRESH_SECRET=base64-48-random-string
